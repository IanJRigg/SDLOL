#include "catch.hpp"
#include "label.h"

TEST_CASE("Label API")
{
    // Needed for normal contstruction
    Window window("Test Case!", 1920UL, 1080UL);
    Renderer renderer(window);

    SECTION("Renderer's constructor")
    {
        
    }
}